app = "solbot1-iajzhw"
primary_region = "ams"

[build]

[mounts]
  source = "sniper_data"
  destination = "/data"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory = "256mb"
  auto_stop_machines = false
  auto_start_machines = false
  min_machines_running = 1

[deploy]
  strategy = "immediate"
